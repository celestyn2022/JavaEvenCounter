# Java Even Number Counter

//This is the Java version of the program originally written in C (Assignment 1). It counts the number of even integers in two arrays.

//The variable `n` was renamed to `fooLength` to better reflect its meaning.

//[Console Output](output.png)

- `Main.java`: Main program logic
- `output.png`: Screenshot of program output
